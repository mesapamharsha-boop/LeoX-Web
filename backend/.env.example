# ==============================================================================
# LEOX BACKEND CONFIGURATION
# Server-only environment variables - Never expose to client
# ==============================================================================

# Server configuration
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173,http://localhost:3000

# Authentication & Admin
JWT_SECRET=leox_jwt_super_secret_production_key_2026
ADMIN_EMAIL=harsha@leox
ADMIN_PASSWORD=leoX@4536

# MongoDB connection string (Optional - fallback embedded JSON store in /data active if empty)
MONGO_URI=
MONGO_DB_NAME=leox

# Email SMTP Notification service (Optional - console preview logger active if empty)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASSWORD=
NOTIFICATION_EMAIL=mesapamharsha@gmail.com

# Cloudinary Media Storage (Optional - local persistent uploads in /public/uploads active if empty)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Google Gemini AI (Server-side only)
GEMINI_API_KEY=
