# ==============================================================================
# LEOX FRONTEND CONFIGURATION
# Client-safe environment variables only (VITE_ prefix required)
# ==============================================================================

# Backend API URL:
# Leave blank to use relative same-origin /api (works in development and production)
# Optional override for separate frontend dev: http://localhost:3000
VITE_API_URL=
